sensitive_words = ("sex", "sexual", "sexualized", "underage", "minors", "massage", "rub", "rubbed", "nude", "abuse", "abusing")
unlem = "‼️"
cesit = set(sensitive_words)

def cesitt_2(text):
    geciyomu = []
    for kelime in cesit:
        if kelime in text:
            geciyomu.append(kelime)
    print(f"Metinde {geciyomu} kelimeleri bulunmaktadır")


def uyari_1(text):
    toplam = 0
    for word in sensitive_words:
        textt = text.split()
        kac = textt.count(word)
        toplam += kac
    
        print(f"Cümledeki {word} kelimesi {kac} kere tekrar ediyor")
    cesitt_2(text)
    def filter_3(text):
        nonlocal kac
        if toplam >= 0 and kac <= 10:
            print(f"File contains {toplam} sensitive word in total")
            print(f"Sensitivity Level:{unlem}")
        elif toplam > 10:
            print(f"File contains {toplam} sensitive word in total")
            print(f"Sensitivity Level:{unlem * 3}\nWarning {unlem} {unlem} {unlem} Disturbing Content")
    filter_3(text)
    
    
    


