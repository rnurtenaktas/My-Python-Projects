sensitive_words = ("sex", "sexual", "sexualized", "underage", "minors", "massage", "rub", "rubbed", "nude", "remove clothes", "abuse", "abusing")
unlem = "‼️"

def count(file):
    words = file.split()
    bulunanlar = []
    for kelime in sensitive_words:
        if words.count(kelime) > 0:
            bulunanlar.append(kelime)   
    return len(bulunanlar), bulunanlar


def filter(file):
    result, liste = count(file)
    if result > 0:
        if result >= 1 and result <= 10:
            print(f"File contains {result} sensitive word")
            print(f"Sensitivity Level:{unlem}")
            print(f"Bulunan kelimeler: {liste}")
            
        elif result > 10:
            print(f"File contains {result} sensitive word")
            print(f"Sensitivity Level:{unlem * 3}")
            print("Warning", unlem, unlem, unlem, "Disturbing Content")

